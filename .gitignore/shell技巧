

//1.tab补全

多级目录可以一直tab，前提是必须没有其他文件或文件夹，如果有其他文件夹，则需要打出几个字母后再tab

//2.history历史命令

列出最近使用的所有命令

//3.!203

使用最近使用列表中编号为203的命令

//4.!h

使用历史命令中最后一个以h开头的命令

//5. | 管道

前一条指令的结果作为后一条指令的输入

//6.ls 命令
ls 列出目录指令：
ls 列出当前目录下的所有文件及文件夹
ls -l 列出当前目录下的内容及详细信息
ls -d a 列出当前目录下的文件名为a的文件或文件夹
ls -ld a 只列出当前目录下的a文件或文件夹和详细信息
ls -R a 递归查看当前目录下的a文件夹

//获取帮助
ls --help 获取ls命令的帮助
man ls 获取ls命令的详细帮助，一般比较复杂，较难看懂,要查看其它命令的详细帮助文档可以把ls换掉来查看
