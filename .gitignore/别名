

//对于一段很长的命令，而又需要经常使用，这时就可以使用别名来提高操作效率

添加别名：
 alias 名称='命令'
 
删除别名：
 unalias 名称

查看别名：
 alias
 
// 压缩包的处理,linux下很多软件都是以压缩包的形式存在的，那么解压和制作压缩包就必须要了解

制作zip压缩包：

 linux下zip压缩不自动递归，所以需要使用
 zip -r 压缩后的文件名 要压缩的文件或文件夹
 
解压zip压缩包：
unzip 文件名

不解压，查看zip压缩包内容:
unzip -l 压缩包名

gz格式的压缩包

制作gz格式压缩包：

tar czf 压缩后的文件名.tar.gz 要压缩的文件或文件夹

解压gz格式的压缩包：

tar xzf 文件名

不解压，查看gz压缩包的内容：
tar tf 文件名


